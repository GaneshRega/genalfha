import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact — Prompt Path | Gen Alfha",
  description:
    "Get support for Prompt Path. Report a bug, request a feature, or reach out to the Gen Alfha team.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
