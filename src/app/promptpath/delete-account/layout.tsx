import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Delete Account — Prompt Path | Gen Alfha",
  description:
    "Request deletion of your Prompt Path account and personal data. We process all requests within 30 days.",
};

export default function DeleteAccountLayout({ children }: { children: React.ReactNode }) {
  return children;
}
